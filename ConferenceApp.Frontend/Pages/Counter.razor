@page "/counter"
@using ConferenceApp.Frontend.Services;

@code {
    protected override async Task OnInitializedAsync() {
        AttendeeService service = new AttendeeService(new HttpClient());
        var attendees = await service.GetAttendees();
        attendees.ToList().ForEach(a => Console.WriteLine(a));
    }
}
