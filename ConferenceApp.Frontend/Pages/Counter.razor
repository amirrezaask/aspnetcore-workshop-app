@page "/counter"
@using ConferenceApp.Frontend.Services;

@code {
    @* protected override async Task OnInitializedAsync() {
        BaseService service = new BaseService(new HttpClient());
        var attendees = await service.GetList<Domain.Attendee>(typeof(Domain.Attendee));
        attendees.ToList().ForEach(a => Console.WriteLine(a));
    } *@
}
